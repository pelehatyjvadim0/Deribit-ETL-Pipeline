<div align="center">
  <h1>DERIBIT-ETL-PIPELINE</h1>
  <p><i>Asynchronous Automated Data Ingestion for Crypto Derivatives</i></p>
  <hr />
</div>

<b>Deribit-ETL-Pipeline</b> - это высокопроизводительный сервис на Python для автоматизированного сбора и архивации рыночных данных с биржи Deribit. Проект построен на асинхронной архитектуре и предназначен для формирования исторических наборов данных (Time-Series) для последующего анализа или трейдинга.

<b>🛠 Технологический стек</b>

    Runtime: Python 3.12 (Asynchronous)

    Web Framework: FastAPI (Lifespan management, DI)

    Task Queue: Celery + Celery Beat (Scheduled tasks)

    Storage: PostgreSQL (SQLAlchemy 2.0 + asyncpg)

    Cache/Broker: Redis

    Data Fetching: aiohttp

<b>🏗 Архитектура ETL</b>

Проект реализует классический цикл обработки данных:

    Extract: Асинхронный клиент DeribitClient выполняет неблокирующие запросы к API биржи.

    Transform: Данные проходят валидацию, таймстампы приводятся к формату BigInt (миллисекунды), формируются объекты DAO.

    Load: Оптимизированная запись в базу данных с использованием пула асинхронных соединений.

Ключевые особенности:

    Event Loop Management: Решена проблема конфликтов asyncio event loop в долгоживущих процессах Celery (через изоляцию циклов и очистку пула соединений).

    Scalability: Благодаря Celery и Redis, система легко масштабируется путем добавления новых воркеров.

    Data Integrity: Использование BigInteger для хранения цен и меток времени исключает ошибки переполнения при работе с высокоточными данными.


<b>📂 Структура проекта</b>



        deribit-etl-pipeline/
        ├── app/
        │   ├── core/           # Конфигурация, настройки БД
        │   ├── currency/       # Логика работы с валютами и DAO
        │   ├── external_api/   # Клиенты для внешних сервисов (Deribit)
        │   ├── tasks/          # Задачи Celery и планировщик Beat
        │   └── main.py         # Точка входа FastAPI
        ├── docker-compose.yml  # Оркестрация сервисов (API, Worker, Redis, DB)
        ├── Dockerfile          # Сборка образа приложения
        ├── .env.example        # Пример переменных окружения
        └── requirements.txt    # Зависимости проекта

<b>🚀 Быстрый старт</b>

Требования

    Docker и Docker Compose

Запуск

    Клонируйте репозиторий:
    Bash

    git clone https://github.com/your-username/deribit-etl-pipeline.git
    cd deribit-etl-pipeline

    Создайте файл .env на основе примера:
    Bash

    cp .env.example .env

    Запустите проект:
    Bash

    docker-compose up --build

После запуска:

    API будет доступно по адресу: http://localhost:8000/docs

    Celery Beat начнет автоматический сбор данных каждую минуту.

<b>🔧 Решенные технические вызовы</b>

    Проблема: При работе Celery Worker возникала ошибка RuntimeError: Task attached to a different loop.

    Решение: Реализована архитектура с использованием asyncio.run() внутри синхронных оберток задач Celery в сочетании с engine.dispose(). Это гарантирует, что каждая итерация ETL-процесса работает в чистом цикле событий с валидными соединениями к БД.

<b>📈 Roadmap</b>

    [🕒] Добавление эндпоинта для выгрузки данных в CSV/JSON.

    [🕒] Покрытие DAO и API Unit-тестами (Pytest).

    [🕒] Интеграция Grafana для визуализации накопленных данных.
